package com.delivce.kikapu.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {

    val Amber = Color(0xFFF5A623)

    val Earth = Color(0xFF2B1A0E)

    // Warm, clean off-white background — softer and less beige than the old Cream.
    val Cream = Color(0xFFF7F6F3)

    val Border = Color(0xFF1A1208)

    val Green = Color(0xFF2D7A3A)

    val Coral = Color(0xFFE8472A)

    // Pure white for card/surface fills, per the reference stat-card treatment.
    val White = Color(0xFFFFFFFF)

    val Gray = Color(0xFF7A6248)

    val LightGray = Color(0xFFE0D8CC)

    val DarkSurface = Color(0xFF38271B)

    // Reference accent palette
    val Blue = Color(0xFF4A90E2)
    val Teal = Color(0xFF4ECDC4)

    // Status colors — single source of truth (previously duplicated in TripComponents.kt)
    val SuccessGreen = Color(0xFF2ECC71)
    val ErrorRed = Color(0xFFE74C3C)
    val InfoBlue = Color(0xFF3498DB)
}